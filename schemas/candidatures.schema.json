{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "candidatures.schema.json",
  "title": "Candidatures à une élection",
  "description": "Qui s'est présenté à une élection, et à quels tours. Document volatil pour une élection à venir, où il change dès qu'une candidature est annoncée, retirée ou validée. Il reste séparé des résultats, qui ne bougent plus une fois le scrutin proclamé, pour qu'une donnée provisoire ne fasse pas bouger une donnée définitive.",
  "type": "object",
  "required": [
    "election",
    "candidatures"
  ],
  "additionalProperties": false,
  "properties": {
    "$schema": {
      "type": "string",
      "description": "Chemin vers ce schéma, pour l'outillage d'édition."
    },
    "election": {
      "type": "string",
      "description": "Identifiant de l'élection, celui du répertoire qui porte ce document.",
      "pattern": "^PR-[0-9]{4}$"
    },
    "candidatures": {
      "type": "array",
      "description": "Une entrée par personne. Une personne ne se présente qu'une fois à une même élection.",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/candidature"
      }
    }
  },
  "$defs": {
    "candidature": {
      "type": "object",
      "description": "Une personne se présentant à une élection. La candidature est à l'élection, pas au tour : on ne se porte pas candidat au second tour, on s'y qualifie.",
      "required": [
        "personne",
        "nom_complet",
        "etats"
      ],
      "additionalProperties": false,
      "properties": {
        "personne": {
          "type": "string",
          "description": "Identifiant interne de la personne. Il la suit d'une élection à l'autre, ce qui permet de rapprocher ses candidatures successives. Volontairement opaque : il ne porte aucune information, donc rien n'oblige jamais à le corriger.",
          "pattern": "^PE-[0-9]{4}$",
          "examples": [
            "PE-0001"
          ]
        },
        "nom_complet": {
          "type": "string",
          "minLength": 1,
          "description": "Nom entier porté lors de cette élection, tel que la source l'écrit. Non découpé en nom et prénom : le découpage est une inférence, fausse dès qu'un nom sort du cas courant — deux prénoms, une particule au milieu, un pseudonyme. La casse suit la source : les décisions du Conseil constitutionnel mettent le patronyme en capitales, pas les articles encyclopédiques. Qui veut la structure la tire de Wikidata, via l'identifiant de la personne."
        },
        "etats": {
          "type": "array",
          "description": "La trajectoire de la candidature : les états par lesquels elle est passée, dans l'ordre, chacun daté et sourcé. L'état courant est le dernier élément — il n'est pas publié à part, pour qu'il ne puisse pas diverger de la trajectoire. Une candidature déclarée puis abandonnée garde ainsi trace de sa déclaration, et une candidature validée garde qui l'avait annoncée.",
          "minItems": 1,
          "items": {
            "$ref": "#/$defs/changement"
          }
        },
        "tours": {
          "type": "array",
          "description": "Les tours auxquels cette candidature a pris part, par numéro croissant, chacun une seule fois. Vide tant qu'aucun tour n'a eu lieu : une candidature déclarée puis retirée n'en a aucun. Ne pas supposer qu'une candidature présente au premier tour l'est au second.",
          "items": {
            "$ref": "#/$defs/participation"
          }
        }
      }
    },
    "changement": {
      "type": "object",
      "description": "Un état par lequel la candidature est passée, à sa date et avec ce qui l'établit.",
      "required": [
        "etat",
        "sources"
      ],
      "additionalProperties": false,
      "properties": {
        "etat": {
          "$ref": "#/$defs/etat"
        },
        "date": {
          "type": "string",
          "format": "date",
          "description": "Date à laquelle la candidature a pris cet état, telle que la source l'établit : la date de la décision, celle de l'annonce. Jamais une date de traitement. Absente quand la source établit l'état sans le dater — ce qui est courant pour un retrait rapporté sans jour précis. L'ordre du tableau fait alors foi."
        },
        "sources": {
          "type": "array",
          "description": "Ce qui établit ce changement d'état.",
          "minItems": 1,
          "items": {
            "$ref": "source.schema.json"
          }
        }
      }
    },
    "participation": {
      "type": "object",
      "description": "La présence d'une candidature à un tour, et ce qui l'établit.",
      "required": [
        "numero",
        "sources"
      ],
      "additionalProperties": false,
      "properties": {
        "numero": {
          "type": "integer",
          "minimum": 1,
          "description": "Rang du tour dans l'élection. Correspond à un tour déclaré par les métadonnées de l'élection."
        },
        "sources": {
          "type": "array",
          "description": "Ce qui établit la présence à ce tour. Pour les élections passées, la décision du Conseil constitutionnel arrêtant la liste des candidats du premier tour, puis celle des candidats habilités au second.",
          "minItems": 1,
          "items": {
            "$ref": "source.schema.json"
          }
        }
      }
    },
    "etat": {
      "description": "Où en est la candidature. Le décompte des parrainages n'est pas un état : il vaut pour toutes les candidatures d'une même période et relèvera d'un document propre.",
      "oneOf": [
        {
          "const": "declaree",
          "description": "Annoncée publiquement, sans validation d'aucune sorte."
        },
        {
          "const": "validee",
          "description": "Figure sur la liste des candidats arrêtée par le Conseil constitutionnel."
        },
        {
          "const": "retiree",
          "description": "La personne a renoncé après avoir annoncé."
        },
        {
          "const": "ecartee",
          "description": "N'a pas été retenue sur la liste officielle, faute de parrainages notamment."
        }
      ]
    }
  }
}
